#pragma once

// Copy to secrets.h and replace these values for the private benchmark LAN.
// secrets.h is ignored repository-wide and must never be committed.
#define TM_WIFI_SSID "replace-me"
#define TM_WIFI_PASSWORD "replace-me"
