#pragma once

/*
 * Copy this file to secrets.h and replace both values with the credentials for
 * the benchmark LAN. secrets.h is ignored by Git. An empty password is valid
 * for an intentionally open test network.
 */
#define TM_WIFI_SSID "YOUR_WIFI_NAME"
#define TM_WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
