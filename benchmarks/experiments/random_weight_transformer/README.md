# Random-weight Transformer experiment

This PlatformIO experiment generates deterministic quantised weights instead
of storing a trained model. Its shape is not an official benchmark case, and no
canonical physical result is currently recorded.

Keep any future firmware changes, validation tools, and raw measurements inside
this directory so the experiment remains self-contained.
