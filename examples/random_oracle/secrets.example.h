#pragma once

// Copy this file to secrets.h and fill in your local network credentials.
// secrets.h is excluded from Git so credentials are never committed.
constexpr char WIFI_SSID[] = "YOUR_WIFI_NAME";
constexpr char WIFI_PASSWORD[] = "YOUR_WIFI_PASSWORD";

